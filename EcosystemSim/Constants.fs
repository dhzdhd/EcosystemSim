namespace EcoSim

open Raylib_cs

module Constants =
    let WIDTH = 1080
    let HEIGHT = 720
    let PASSIVE_COLOR = Color.GREEN
    let AGGRO_COLOR = Color.RED
    let DISEASED_COLOR = Color.YELLOW
    let INIT_BLOB_AMOUNT = 10
    let INIT_FOOD_TIMER = 50
